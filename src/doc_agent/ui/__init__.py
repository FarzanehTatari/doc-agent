"""Streamlit web UI for doc-agent.

Launch with:
    doc-agent ui

The UI is an optional install — `pip install -e ".[ui]"` pulls Streamlit.
The entrypoint is `home.py`; multi-page navigation is provided by the
`pages/` directory next to it (Streamlit convention).
"""
